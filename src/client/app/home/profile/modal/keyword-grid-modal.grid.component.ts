import { Component } from '@angular/core';

/**
 * A Sample of how simple it is to create a new window, with its own injects.
 */
@Component({
  moduleId: 'modal-content',
  selector: 'sd-keyword-grid-modal',
  styles: [],
 template: ``
})
export class KeywordGridModalComponent {}
